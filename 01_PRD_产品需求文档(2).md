# 01 PRD：AI知识库管理中台产品需求文档

## 1. 产品定位
AI知识库管理中台是面向企业内部Agent体系的AI资产管理平台，用于统一管理知识、工具、Skill、Prompt、存量Agent和调用权限，为人事、财务、团品提报、供应链、招扶商、运营、商学院等Agent提供标准化、可复用、可管控的能力供给。

## 2. 目标用户

### 2.1 管理类用户
- AI平台管理员：负责中台配置、权限、系统监控、模型配置。
- 知识管理员：负责知识库创建、文档审核、版本发布、知识下线。
- 能力管理员：负责工具、Skill、Prompt、Agent能力注册与维护。
- 安全合规管理员：负责敏感数据、审计日志、调用风险和合规策略。

### 2.2 业务类用户
- 人事Agent Owner
- 财务Agent Owner
- 团品提报Agent Owner
- 供应链Agent Owner
- 招扶商Agent Owner
- 运营Agent Owner
- 商学院Agent Owner
- 多Agent协作系统负责人

### 2.3 调用方
- 单个业务Agent
- 多Agent协作系统中的主控Agent
- 多Agent协作系统中的子Agent
- 企业内部应用，如OA、BI、CRM、门店系统、培训系统

## 3. 核心业务场景

### 场景A：业务Agent动态获取能力
运营智能体收到“生成门店直播方案”任务后，不再加载所有工具，而是调用中台能力检索接口，查询“门店运营、直播话术、合规审核”等能力。中台按权限返回可用能力，Agent再按需调用。

### 场景B：多Agent协作使用统一能力库
主控Agent接收复杂办公任务后，先调用中台获取授权能力列表，再拆分任务给人事、财务、供应链等子Agent。每个子Agent在执行时通过统一SDK检索和调用中台能力。

### 场景C：知识更新一处，全量Agent同步生效
产品运营SOP更新后，只需要在中台更新知识库并发布新版本，所有授权Agent在下一次检索时自动使用新版本，无需逐个修改Agent代码。

### 场景D：Skill沉淀复用
商学院团队把“课程大纲生成”“培训测验题生成”沉淀为Skill。后续运营Agent或人事Agent需要培训材料时可以直接调用，不必重复开发。

### 场景E：敏感能力按权限隔离
财务预算测算工具仅允许财务Agent、主控Agent在特定流程中调用；运营Agent即使搜索相关能力，也只能看到脱敏知识或无权提示。

## 4. 功能模块

### 4.1 能力注册中心
功能：
- 注册知识库、工具、Skill、Prompt、存量Agent。
- 配置能力名称、描述、标签、适用场景、输入输出Schema、调用地址、认证方式、版本号、Owner。
- 支持能力状态：草稿、测试中、已发布、灰度中、停用、废弃。
- 支持能力检索：关键词、标签、语义、权限、场景组合过滤。
- 支持能力推荐：根据任务描述返回推荐能力和推荐调用顺序。

验收标准：
- Agent提交任务关键词后，中台可返回不超过Top K的授权能力。
- 不同Agent角色获得的能力结果不同。
- 能力变更后有版本记录和审计记录。

### 4.2 知识资产管理
功能：
- 创建知识库：产品知识库、运营SOP库、FAQ库、合规规则库、门店案例库等。
- 文档接入：上传、URL采集、数据库同步、API同步、企业网盘同步。
- 文档解析：PDF、Word、Excel、PPT、Markdown、HTML、图片OCR。
- 知识切片：按标题、段落、表格、语义、规则进行切分。
- 知识索引：关键词索引、向量索引、混合索引。
- 知识权限：库级、文档级、切片级权限。
- 知识版本：草稿、审核、发布、回滚、下线。
- 知识质量：重复检测、过期检测、冲突检测、召回测试。

验收标准：
- 上传文档后可查看解析状态、切片数、Embedding状态。
- Agent检索结果必须包含来源、版本、命中片段和置信度。
- 知识库版本回滚后检索结果同步变化。

### 4.3 Skill与Prompt资产化
功能：
- Prompt模板管理：变量、版本、适用Agent、评估集、发布状态。
- Skill封装：输入Schema、处理逻辑、输出Schema、模型配置、工具依赖。
- Skill调试台：输入样例、运行日志、质量评分。
- Skill发布：灰度、回滚、审批、Owner。
- Skill复用：多个Agent按权限调用同一Skill。

验收标准：
- 同一个直播话术Skill可被运营Agent、商学院Agent调用。
- Prompt修改后可灰度给部分Agent。
- 每次Skill调用都有版本号与日志。

### 4.4 智能体统一纳管
功能：
- Agent注册：名称、角色、业务线、Owner、调用凭证、权限标签。
- Agent发现：主控Agent可发现可协作的子Agent。
- Agent健康检查：在线状态、平均响应时长、失败率。
- Agent能力画像：该Agent擅长任务、可调用能力、限制条件。
- Agent调用审计：谁调用、何时调用、输入输出摘要、耗时、错误。

验收标准：
- 主控Agent可查询“供应链领域可用Agent”。
- 停用某Agent后，主控Agent不再能调度该Agent。
- 每个Agent的能力调用链路可追踪。

### 4.5 权限与治理
功能：
- 多租户/业务线隔离。
- RBAC：按角色授权。
- ABAC：按部门、区域、Agent类型、数据密级、任务场景授权。
- 数据脱敏：手机号、身份证、薪资、合同金额等敏感字段。
- 审批流：高风险工具发布、敏感知识发布、跨部门授权。
- 审计日志：检索、调用、发布、审批、导出、删除。

验收标准：
- 未授权Agent无法检索或调用财务工具。
- 敏感字段在低权限Agent返回结果中被脱敏。
- 管理员可按Agent、能力、时间范围查询调用记录。

### 4.6 管理后台
功能页面：
- 首页总览：能力数量、知识库数量、调用量、失败率、风险告警。
- 能力目录：检索、筛选、查看、注册、编辑、发布、下线。
- 知识库管理：文档上传、解析、切片、索引、权限、版本。
- Skill/Prompt管理：模板、变量、版本、评估、灰度发布。
- Agent管理：注册、权限、健康状态、调用日志。
- 审批中心：发布审批、授权审批、下线审批。
- 监控中心：调用链、Token、耗时、异常、成本。
- 评估中心：召回率、答案准确率、幻觉率、合规通过率。

## 5. 非功能需求

| 项 | 要求 |
|---|---|
| 可用性 | 核心接口可用性 >= 99.5%，生产目标 >= 99.9% |
| 性能 | 能力检索P95 < 500ms；知识检索P95 < 1200ms；能力调用按具体能力定义SLA |
| 安全 | 全链路鉴权、敏感数据脱敏、审计留痕 |
| 扩展性 | 支持新增能力类型，支持新Agent零侵入接入 |
| 可观测 | 每次调用有trace_id，支持链路追踪 |
| 兼容性 | 支持REST API，预留MCP/A2A协议适配 |
| 可维护 | 能力、Prompt、知识、Agent均有Owner与生命周期 |

## 6. 版本路线

### V1：生产可用基础版
- 能力注册中心
- 知识库管理与RAG检索
- Agent SDK
- 基础RBAC
- 调用日志
- 管理后台基础页面

### V2：企业治理增强版
- ABAC权限
- 知识版本回滚
- Prompt/Skill灰度发布
- 调用链追踪
- 质量评估中心
- 多Agent能力推荐

### V3：智能运营版
- 自动知识冲突检测
- 自动沉淀优质案例
- 能力推荐排序模型
- 成本优化与模型路由
- MCP/A2A标准协议网关
